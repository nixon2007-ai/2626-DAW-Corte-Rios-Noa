# Copia este archivo como ".env" (sin ".example") en la raiz del proyecto
# y pon aqui los datos reales de TU PostgreSQL local.
# El archivo .env nunca debe subirse a GitHub (agregalo a .gitignore).
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=pon_aqui_tu_clave_real
DB_NAME=gastrobar_db
# Deja esto vacio/comentado en local. Solo Render lo define automaticamente.
# DATABASE_URL=